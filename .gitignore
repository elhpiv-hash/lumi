# Исходники, из которых собираются спрайты. В игру попадает только уменьшенная
# копия, зашитая в src/assets/ как data-URI, — полноразмерные оригиналы
# в репозиторий не кладём.
assets/*.jpg
assets/*.jpeg
assets/*.png
